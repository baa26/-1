&НаКлиенте
Процедура ЗаполнитьАдрес(Команда)
	Адрес = Строка(Город) + ", "
				+ Строка(Улица) + ", " + Строка(Дом) + ", "
				+ Строка(Квартира);
	Оповестить("ЗаполнитьАдрес", Адрес);
	ЭтаФорма.Закрыть();
КонецПроцедуры



